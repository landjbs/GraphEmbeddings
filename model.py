import numpy as np
import tensorflow as tf

def gen_data(shape, mainNodes):
    assert np.sqrt(shape) >= mainNodes, 'shape should be geq than main nodes'
    dataMatrix = np.random.zeros(shape=(shape, shape))
    cityLocs
    return np.
